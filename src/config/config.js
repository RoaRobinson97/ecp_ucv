// /src/config/config.js

export const CONFIG = {
    API_URL: 'https://api.tudominio.com/v1',
    // Cambia esto a 'false' para usar la API real
    USE_MOCK_DATA: true, 
    // Otros ajustes globales...
};