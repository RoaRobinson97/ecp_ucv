server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Esta línea es clave para React (SPA): si no encuentra un archivo físico, redirige al index.html
        try_files $uri $uri/ /index.html;
    }
}